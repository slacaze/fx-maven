function addsandbox()
    if ~isempty( ver( 'maven' ) )
        fx.maven.command.addsandbox();
    else
        thisPath = fileparts( mfilename( 'fullpath' ) );
        addpath( fullfile(...
            thisPath,...
            'code',...
            'myapp' ) );
        addpath( fullfile(...
            thisPath,...
            'test' ) );
    end
end
